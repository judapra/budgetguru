import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.studiocompass.budgetguru',
  appName: 'Budget Guru',
  webDir: 'out'
};

export default config;
